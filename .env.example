# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=postgres
POSTGRES_PORT=5432

# Network Configuration
# 0.0.0.0 = allow external connections from any IP
# 127.0.0.1 = localhost only (more secure)
POSTGRES_HOST_IP=0.0.0.0

# Production Settings
# Change these values for production deployment
# Recommended: Use firewall rules to restrict IP access
